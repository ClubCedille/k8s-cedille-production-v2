apiVersion: v1
kind: Service
metadata:
  name: habitek-svc
  namespace: habitek
  labels:
    app: habitek
spec:
  ports:
    - name: http
      targetPort: 80
      protocol: TCP
      port: 80
  type: ClusterIP
  selector:
    app: habitek
    tier: frontend
